# Credentials


- BigQuery ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/google-bigquery))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'bigquery',
    		credentials: {
    			projectId: '...',
    			credentials: {
    				"type": "service_account",
    				"project_id": "...",
    				"private_key_id": "...",
    				"private_key": "...",
    				// ...
    			}
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- Clickhouse ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/clickhouse))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'clickhouse',
    		credentials: {
    			database: '...',
    			host: '...',
    			user : '...',
    			password : '...',
    			port: 8443
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- Druid ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/druid))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'druid',
    		credentials: {
    			url: '...',
    			user: '...',
    			password: '...',
    			// database: '...',
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- MongoDB using [BI-Connector](https://www.mongodb.com/docs/bi-connector) ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/mongodb))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'mongobi',
    		credentials: {
    			database: '...',
    			host: '...',
    			user: '...',
    			password: '...',
    			port: 3307,
    			ssl: true,
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- MySQL / MariaDB ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/mysql))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'mysql',
    		credentials: {
    			database: '...',
    			host: '...',
    			user: '...',
    			password: '...',
    			port: 3306,
    			ssl: true
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- Postgres ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/postgres))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'postgres',
    		credentials: {
    			database: '...',
    			host: '...',
    			user: '...',
    			password: '...',
    			port: 5432,
    			ssl: true // or { rejectUnauthorized: false }
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- Redshift ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/aws-redshift))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'redshift',
    		credentials: {
    			database: '...',
    			host: '...',
    			user: '...',
    			password: '...',
    			port: 5439,
    			ssl: true
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- Snowflake ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/snowflake))
    
    ```tsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'snowflake',
    		credentials: {
    			account: '...',
    			warehouse: '...',
    			database: '...',
    			username: '...',
    			password: '...',
    			// role: '...',
    			// region: '...',
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    
    ```
    
- MS SQL / SQL Server ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/ms-sql))
    
    ```jsx
    const apiKey = '...';
    const connectionName = 'my-db';
    
    /** 
     * creates a db connection named `my-db` 
     *  (which you can use in your model by setting the `data_source` field to `my-db`)
     */
    const resp = await fetch(`https://api.embeddable.com/api/v1/connections`, {
    	method: 'POST', // POST = CREATE
    	headers: {
    		'Content-Type': 'application/json',
    		'Accept': 'application/json',
    		'Authorization': `Bearer ${apiKey}` /* keep your API Key secure */
    	},
    	body: JSON.stringify({
    		name: `${connectionName}`,
    		type: 'mssql',
    		credentials: {
    			server: '...',
    			port: 1433,
    			user: '...',
    			password: '...',
    			database: '...',
    			options: { 
    				// trustServerCertificate: true 
    			}
    		}
    	})
    });
    
    console.log(`${resp.status} ${resp.statusText}`);
    const json = await resp.json();
    console.log(json);
    ```
    
- Trino ([Cube docs](https://cube.dev/docs/product/configuration/data-sources/trino))